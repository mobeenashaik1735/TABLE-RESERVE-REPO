{
  "name": "tablereserve-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "NODE_ENV=development node backend/server.js",
    "build": "npm run build --workspace=frontend",
    "start": "NODE_ENV=production node backend/server.js",
    "lint": "npm run lint --workspace=frontend"
  }
}
